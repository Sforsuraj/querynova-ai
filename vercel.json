{
  "version": 2,
  "installCommand": "cd frontend && npm install --frozen-lockfile",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" }
  ]
}
